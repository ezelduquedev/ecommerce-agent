services:
  - type: web
    name: shopbot-ecommerce-agent
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: MODEL
        value: llama-3.3-70b-versatile
